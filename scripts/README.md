# Terminal kill helpers

Use PowerShell to stop the active terminal-related processes from the repository root:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\kill-terminals.ps1
```

You can also create a shortcut or alias in your shell profile for quicker access.
